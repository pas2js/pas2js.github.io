program projDBNavigator;

{$mode objfpc}

uses
  SysUtils, Classes, Types, Math, Web, JS, uDBNavigator;

var
  DBNavigator: TDBNavigator;

begin
  DBNavigator:= TDBNavigator.Create;

end.
